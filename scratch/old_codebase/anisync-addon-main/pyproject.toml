[project]
name = "anisync"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "pymongo",
    "httpx",
    "quart>=0.20.0",
    "uvicorn>=0.29.0",
    "Pillow>=10.0.0",
]

[tool.uv]
package = false
